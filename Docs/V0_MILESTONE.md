# v0 마일스톤

이 문서는 v0 플레이어블 빌드 완료까지의 작업을 순서대로 추적합니다.

- 기준일: 2026-02-09
- 목표: `RunScene`에서 모험가-몬스터 루프를 실제로 플레이 가능한 상태까지 완성
- 진행률(체크리스트 기준): `15 / 21` (`71%`)
- 진행 바: `[##############------]`
- 사용 규칙: 위에서 아래 순서로 진행, 체크는 완료 기준 충족 시에만 갱신

## M0. 설계 잠금

1. [x] 용어 사전 확정(스킬/구조물/칙령/몬스터/체력/공격)
2. [x] 턴 최소 사이클(P0~P6) 문서화
3. [x] 스킬 3종 + 쿨다운 값 확정
4. [x] 몬스터 최소 스키마(`action_pool`) 1차 확정
5. [x] 효과 포맷(`effect_bundle`) 확정
6. [x] `effect_type`별 필수 `params` 규칙 확정
7. [x] `action_pool` 상세 규칙 확정(행동 개수/가중치/`prep_turns` 범위)
8. [x] 모험가 최소 스키마 확정(굴림 주사위 수/고유 효과/장비 슬롯)

## M1. 데이터/런타임 스키마 구현

9. [x] `EnemyDef`/`ActionDef`/`EffectBundle` DTO 구현
10. [x] `AdventurerDef`/`SkillDef` DTO 구현
11. [x] 런타임 상태 모델 구현(`EnemyState`, 턴 상태, 쿨다운 상태)
12. [x] v0 샘플 데이터 작성(`Enemies.json`, `Skills.json`, `Adventurers.json`)

## M2. 코어 루프 구현

13. [x] 런 시작 초기화 구현(`Stability` 20, `Gold` 0, 동일 위협 예산의 사전 제작 프리셋 `3`개 중 랜덤 1개 소환)
14. [x] 턴 오케스트레이터 구현(P0~P6 진행, 보드 전멸 시 `P1` 프리셋 재소환 포함)
15. [x] P2 모험가 배치 규칙 구현(귀속/이동 제한/경고 조건 포함)
16. [ ] P3 굴림 + P4 공격/효과 적용 구현(모험가 순차 처리 루프 기준으로 재구현)
17. [ ] P5 몬스터 행동 발동(`action_pool`) 구현
18. [ ] 보상/안정도/게임오버 정산 구현

## M3. UI/플레이테스트 준비

19. [ ] 보드 UI 구현(몬스터 체력/행동/준비 카운트, 모험가 배치 상태, 스킬 쿨다운)
20. [ ] 플레이 루프 UX 구현(미배치 모험가 경고, 로그, 재시작 흐름)
21. [ ] 플레이어블 빌드 완료 + 수동 10회 테스트 시작

## v0 완료 기준

- M0~M3의 21개 항목이 모두 체크됨
- 시작부터 게임오버까지 단일 런이 끊김 없이 진행됨
- 10회 수동 플레이 테스트를 수행할 수 있는 상태

