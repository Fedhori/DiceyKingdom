# v0 마일스톤

이 문서는 v0 플레이어블 빌드 완료까지의 작업을 순서대로 추적합니다.

- 기준일: 2026-02-11
- 목표: `GameScene`에서 모험가-상황(Situation)-기한(Deadline) 루프를 실제로 플레이 가능한 상태까지 완성
- 진행률(체크리스트 기준): `13 / 14` (`93%`)
- 진행 바: `[###################-]`
- 사용 규칙: 위에서 아래 순서로 진행, 체크는 완료 기준 충족 시에만 갱신

## 리워크 전환 메모 (2026-02-11)

- 기존 `Enemy/action_pool` 기반 체크리스트(기존 `23 / 24`)는 레거시 기준으로 동결합니다.
- 신규 v0 기준은 아래 `Situation/요구치/기한/성공보상/실패효과` 구조를 기준으로 재작성합니다.

## M0. 설계 잠금 (Situation 전환)

1. [x] 용어 사전 전환 확정(`Enemy -> Situation`, `Health -> Requirement`)
2. [x] 상황 최소 스키마 확정(`기한`, `성공 보상`, `실패 효과`, `실패 잔류 모드(비활성)`)
3. [x] 성공/실패 판정 시점 확정(`성공: P4 즉시`, `실패: P5`)
4. [x] 실패 후 기본 제거 규칙 확정(실패 잔류 로직은 코드 경로만 유지)
5. [x] 초기 `3`개 + 4턴 주기 `3`개 생성 + 균등 랜덤 규칙 확정

## M1. 데이터/런타임 스키마 구현

6. [x] `SituationDef`/`SituationState` DTO 및 런타임 상태 모델 구현
7. [x] `action_pool`/행동 카운트 의존 필드 제거 또는 비활성화
8. [x] 상황 데이터 작성(`Situations.json` 및 생성 풀 데이터)
9. [x] 스킬 효과 타입 전환(`enemy_health_delta -> situation_requirement_delta`)

## M2. 코어 루프 구현

10. [x] P1 생성 로직 구현(런 시작 3개 + 4턴마다 3개 생성, 균등 랜덤, 보드 상한 없음)
11. [x] P4 성공 로직 구현(요구치 0 이하 즉시 성공 보상 + 제거)
12. [x] P5 실패 로직 구현(기한 감소 -> 실패 효과 -> 기본 제거, `reset_deadline` 잔류 분기 지원)
13. [x] 안정도/골드 정산 및 게임오버 판정 재검증

## M3. UI/플레이테스트 준비

14. [ ] 우측 패널/로그/UI 문구를 상황/요구치/기한 기준으로 전환 후 10회 수동 테스트 시작

## v0 완료 기준

- M0~M3의 14개 항목이 모두 체크됨
- 시작부터 게임오버까지 단일 런이 끊김 없이 진행됨
- 10회 수동 플레이 테스트를 수행할 수 있는 상태

