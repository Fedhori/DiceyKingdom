# 게임 아이디어 백로그

이 문서는 떠오르는 아이디어를 빠르게 기록하고, 구현 전 후보를 정리하기 위한 백로그입니다.
구조화된 현재 설계 기준은 `Docs/GAME_STRUCTURE.md`를 우선합니다.

## 플레이테스트 인사이트 로그

### 2026-02-09 밤 플레이테스트 요약

- 관찰 1: "주사위 자체만 분배" 루프는 기대보다 흥미가 낮았습니다.
  - 주사위 수가 적을 때:
    - 시너지 밀도가 낮고 결과가 단순 합산으로 수렴
    - 실패 체감이 "도전"보다 "불쾌한 악운"으로 인식됨
  - 주사위 수가 많을 때:
    - 정보량/선택지가 과도해져 선택 피로 유발
    - 개별 재미보다 기대값 계산 작업으로 플레이가 경직됨
- 관찰 2: "결과가 불확실한 자원"을 분석/직관/계산으로 분배하는 핵심 컨셉은 여전히 유효 가능성이 있습니다.
- 가설 1: 분배 단위를 주사위 단일체가 아닌 "모험가" 패키지로 바꾸면 의사결정 밀도를 높이면서도 정보량을 통제할 수 있습니다.
  - 예시 구성: 굴림 주사위 수, 고유 효과, 장비 슬롯
- 가설 2: 끊김 없는 단일 누적 스테이지는 역전 서사를 만들기 어렵고, 파워 열세 구간에서 붕괴가 급격합니다.
  - 개선 방향: 스테이지 단위 진행 + 적(기존 Situation) 액션의 준비시간/발동 구조
- 프로세스 결론:
  - "세세한 기획 선작성"보다 "빠른 플레이어블 v0 빌드"로 핵심 가설을 먼저 검증해야 합니다.
  - 세부 수치/규칙 확정은 v0 검증 이후로 지연합니다.

### v0 검증 목표(우선순위)

- 목표 문장:
  - "결과가 불확실한 자원을 플레이어가 분석/직관/계산으로 분배하는 행위가 반복적으로 재미를 제공하는가?"
- 1차 합격 기준(확정):
  - 플레이 중/종료 후 "재미 신호"가 명확히 감지되는가
  - 약 `10`회 플레이에서도 재미 신호가 거의 없으면 아이디어 중단 후보로 판정
- 1차 검증 포인트:
  - 턴당 선택 피로(과부하) 없이 의미 있는 고민이 발생하는가
  - 악운 체감이 불쾌감으로 치우치지 않고 대응 가능성으로 인식되는가
  - 빌드/단위 조합이 단순 기대값 계산을 넘어선 질적 차이를 만드는가

### v0 가드레일(초안)

- 분배 단위가 모험가일 경우, 단위 수는 `4`로 고정합니다.
- 이번 v0 빌드는 모험가 <-> 스킬 시너지 검증이 목적이므로, 스킬 최소 세트 3종을 기본 보유로 시작합니다.
  - 원하는 모험가 리롤(쿨다운 `1`)
  - 원하는 주사위 눈 +1(쿨다운 `1`)
  - 지정 상황 요구치 -2(쿨다운 `2`)
- 스킬 규칙은 "개별 쿨다운 + 각 스킬 턴당 1회 사용"으로 고정합니다.

### 실험 전략 결정(확정)

- 이번 실험은 `선택지 A(분배 단위만 전환)`로 진행합니다.
  - 분배 단위: 모험가
  - 진행 구조: 연속 턴 보드 유지 + 런 시작 `3`개 + `4`턴마다 `3`개(`균등 랜덤`) 생성
- 실험 빌드 분리는 이번 사이클에서 수행하지 않습니다.
  - 결정 이유: 우선 분배 단위 변경 효과를 빠르게 확인하고, 스테이지 구조는 다음 사이클에서 별도 전환 가능
- 수용하는 리스크:
  - 연속 누적 구조 고유 문제(말리는 런의 급격한 붕괴)가 남아 결과를 일부 왜곡할 수 있음
- 완화책:
  - 플레이테스트마다 "재미 신호 약화 원인 태그"를 수동 기록(코드 로그 기능은 v0 범위에서 제외)
  - 권장 태그: `과부하`, `악운`, `무기력`, `규칙이해`, `템포저하`

### v0 런 파라미터 잠금(확정)

- 초기 `Stability`: `20`
- 초기 `Gold`: `0`
- 런 시작 시 초기 상황 `3`개 생성
- 상황 풀은 사전 제작 데이터에서 `균등 랜덤` 선정
- 턴당 신규 생성 규칙: `4`턴마다 상황 `3`개 생성
- 보드 최대 상황 수: 상한 없음

### 2026-02-11 리워크 확정(우선순위 재미 강화)

- 결론: `[채택]`
- 확정 규칙:
  - `몬스터(Enemy)` 명칭을 `상황(Situation)`으로 전환
  - `체력(HP)` 명칭을 `요구치(Requirement)`로 전환
  - `행동(action)` / `행동 풀(actionPool)` 시스템은 v0 범위에서 폐기
  - 각 상황은 `기한`, `성공 시 보상`, `실패 시 효과`를 가짐
  - 성공 판정: 요구치가 0 이하가 되는 즉시 성공 처리(보상 획득 후 제거)
  - 실패 판정: `P5 Settlement`에서 기한 감소 후 0 이하이면 실패 처리
  - 실패 기본 처리: 실패 효과 적용 후 보드에서 제거
  - 실패 잔류는 기본값 비활성(`remove`)로 유지하되, 필요 시 `resetDeadline` 실험을 허용
  - 신규 상황 생성 규칙: 런 시작 시 `3`개 생성 + `4`턴마다 `3`개 생성, 보드 상한 없음
  - 생성 대상 선정 방식: 상황 풀에서 `균등 랜덤`
  - 기존 스킬 "지정 몬스터 피해 2"는 "지정 상황 요구치 -2"로 전환
- 채택 근거:
  - "해결 vs 포기"를 실질 선택으로 만들고 우선순위 의사결정의 재미를 강화
  - 실패 상황의 무한 잔류 압박을 제거해 정답형 플레이(기한 임박 우선 처리) 고착을 완화

## 설계 결정 로그

### 2026-02-12 - 요구치 감소형 전투 폐기, 주사위 대결형 전투로 전환

- 최종 확정:
  - 전투 해석을 `상황 요구치 감소`에서 `주사위 대결`로 전환
  - 에이전트/상황 모두 `diceFaces` 배열을 보유
  - 에이전트 주사위 1개와 상황 주사위 1개를 선택해 테스트
  - 성공(`agentRoll >= situationRoll`) 시 상황 주사위 파괴
  - 에이전트 주사위는 성공/실패와 무관하게 시도 즉시 소모
  - 상황 주사위가 0개가 되는 즉시 성공 처리(보상 적용 + 제거)
  - 한 에이전트의 주사위를 모두 소모하면 소진 후 다음 에이전트 처리
  - 기한/실패 효과 정산은 유지
  - 스킬/룰은 전환 단계에서 임시 비활성
- 채택 이유:
  - 단순 합산 공격보다 선택 단위(에이전트 die vs 상황 die) 자체의 의사결정을 강화
  - 상황별 대응 우선순위 판단을 더 직접적인 리스크-보상 선택으로 전환
- 상태:
  - `[채택]`

### 2026-02-12 - Agent ID dot 포맷 고정 + nameKey 제거

- 최종 확정:
  - `AgentDef.agentId`는 `agent.warrior`처럼 dot 포맷으로 사용
  - `AgentDef`에서 `nameKey`를 제거
  - Agent 이름 로컬라이즈 키는 `{agentId}.name` 파생 규칙으로 고정
  - rule 설명 키는 기존대로 `{agentId}.rule.{index}` 유지
- 채택 이유:
  - ID/로컬라이즈 키 규칙을 단일화해 데이터 작성 규칙을 단순화
  - 중복 필드(`nameKey`) 제거로 유지보수 비용 및 불일치 리스크 감소
- 상태:
  - `[채택]`

### 2026-02-12 - 모험가 효과 하드코딩 제거 및 데이터 기반 효과 시스템 전환

- 최종 확정:
  - 모험가 효과의 `agentId` 분기 하드코딩을 제거
  - 효과 로직은 데이터(`rules`) + 공통 룰 실행기 기반으로 통합
  - 각 rule은 `trigger - condition - effect` 3요소로 구성
  - 트리거는 `onRoll`, `onCalculation`으로 고정
  - `onRoll`은 기본 롤 + 전체 리롤 계열에만 적용(단일 주사위 리롤 제외)
  - `onCalculation`은 ATK 계산 시마다 호출(카드 갱신/공격 확정 포함)
  - 룰 실행은 `rules` 배열 인덱스 오름차순 live 평가
  - 트리거 1회 처리 중 같은 rule은 최대 1회만 발동
  - rule 설명 키는 `Agent` 테이블의 `{agentId}.rule.{index}` 구조 사용
  - 설명용 인자 구성은 `RuleTextArgsBuilder` 단일 빌더로 통합하고, Smart Format 인자는 평탄화 키를 사용
  - JSON 키/효과 타입/params 키는 전역 camelCase로 통일(기존 JSON도 마이그레이션)
  - 룰/로컬라이즈 검증은 로드 시 1회 경고 + 에디터 메뉴 검증을 동일 함수로 제공
  - 하드코딩이 불가피한 경우는 사용자 사전 허가를 받은 케이스로만 제한
- 채택 이유:
  - 신규 모험가/효과 추가 시 코드 수정 비용과 회귀 리스크를 줄이기 위함
  - 룰 변경을 데이터 수정 중심으로 전환해 반복 실험 속도를 높이기 위함
- 상태:
  - `[채택]`

### 2026-02-12 - 프로토타입 개선 범위 축소(백로그 우선)

- 최종 확정:
  - 프로토타입에서는 UI/시스템 고도화 항목을 즉시 구현하지 않음
  - 고도화 항목은 `BACKLOG` 기록으로만 관리
  - 현 시점 우선순위는 시작 모험가 세팅(전사/궁수/마법사/도적) 적용
- 채택 이유:
  - v0 핵심 검증 목표(모험가 중심 전투 루프)를 빠르게 확인하기 위함
  - 구현 범위를 과도하게 넓히면 검증 템포가 저하될 리스크가 큼
- 상태:
  - `[채택]`

### 2026-02-12 - 시작 모험가 4종 교체 및 롤 이벤트 고유효과 규칙

- 최종 확정:
  - 시작 모험가는 `전사/궁수/마법사/도적` 4종으로 교체
  - 기본 장비 슬롯은 시작 4종 모두 `2`로 통일
  - 동률 처리 규칙은 좌->우 인덱스 우선
  - 고유효과 적용 순서는 `롤 결과 확정 -> 고유효과 적용 -> UI 갱신 -> Adjustment 스킬`
  - 전사/궁수/도적의 고유효과는 리롤 포함 모든 롤 이벤트에서 발동
  - 단일 주사위 리롤에는 전사/궁수/도적 고유효과 재트리거를 적용하지 않음
  - 마법사 효과는 `6 이상`인 주사위 수만큼 `최종 피해 +6`을 부여하며, 주사위 눈 값 자체는 바꾸지 않음
  - 마법사 최종 피해 보너스는 스냅샷 고정이 아니라, `Adjustment`에서 주사위 값이 바뀌면 즉시 재계산
  - UI 카드 본문 공격력 표기는 `ATK 18 (12 + 6)` 형식으로 고정
  - 마법사 보너스는 `Adjustment` 변경마다 카드에 실시간 갱신
  - 툴팁 포맷은 공통으로 `기본공격 X + 고유보너스 Y = 최종피해 Z`
  - 툴팁 숫자 색상은 `기본공격/고유보너스/최종피해` 역할별 고정값으로 유지하고 `Colors.cs`에서 중앙 관리
  - 툴팁 표시 지연은 고정값 `0.2s`로 유지
  - 툴팁 가능 아이콘은 사용하지 않음
  - 보정 근거(예: `6 이상 주사위 1개 × +6`)는 툴팁에만 표시하고, 별도 디버그 로그는 추가하지 않음
- 채택 이유:
  - 모험가 정체성을 명확히 하면서도 짧은 규칙으로 즉시 이해 가능한 전투 개성을 제공
  - 롤 이벤트 기준의 고정된 처리 순서로 디버깅/UX 일관성을 확보
- 상태:
  - `[채택]`

### 2026-02-11 - 카드 View 인스펙터 직결 참조 규칙 고정

- 최종 확정:
  - `SituationController`, `AgentController`의 UI/입력 참조는 인스펙터 직결(`SerializeField`)만 사용
  - 카드 생성 시 매니저(`SituationManager`, `AgentManager`)는 프리팹의 View 컴포넌트를 필수로 요구
  - 런타임 자동 참조 탐색(`Find*`, `GetComponent*`) 및 자동 컴포넌트 추가(`AddComponent`)는 카드 View에서 사용하지 않음
- 채택 이유:
  - 프리팹 기반 UI의 소유권을 명확히 해 렌더 깨짐/참조 유실 원인 추적을 단순화
  - 코드가 프리팹 레이아웃/구성 요소를 덮어쓰지 않도록 강제해 유지보수 안정성 확보
- 상태:
  - `[채택]`

### 2026-02-11 - 런 보드 UI Manager/View 분리 + 요구치 표기 정리

- 최종 확정:
  - 상황 카드의 `HpText` 용어를 `RequirementText`로 교체
  - 상황 카드 요구 수치 표시는 `SituationController`에서 직접 바인딩
  - 모험가/상황 카드 모두 프리팹 View 컴포넌트가 렌더링/입력 배선을 담당
  - 런타임 로직은 `Managers/Agent(AgentManager)`, `Managers/Situation(SituationManager)`에서 담당
- 채택 이유:
  - 용어 일관성(`HP` 제거, `요구치` 통일) 확보
  - 패널 매니저와 카드 뷰 책임 분리로 유지보수성과 디버깅 효율 개선
  - 카드 프리팹 자체가 자신의 UI를 관리해 씬 결합도 감소
- 상태:
  - `[채택]`

### 2026-02-11 - 타깃 스킬 UX/실패 잔류 검증 완화

- 최종 확정:
  - `situationRequirementDelta` 스킬은 `Q/W/E/R` 또는 슬롯 클릭으로 선택 후, 상황 카드 클릭으로 사용
  - `failurePersistMode` 데이터 검증은 `remove | resetDeadline` 둘 다 허용
- 기본 정책:
  - 현재 밸런스 기본값은 `remove`
  - 필요 시 개별 상황 데이터에서 `resetDeadline`을 실험적으로 활성화 가능
- 채택 이유:
  - 타깃 필요 스킬의 사용 동선을 실제 플레이에서 완결
  - 실패 잔류 실험을 코드 수정 없이 데이터만으로 빠르게 비교 가능
- 상태:
  - `[채택]`

### 2026-02-09 - 롤 후 할당 방식 기각

- 대상 변경안:
  - 기존: 모험가를 적에 할당 -> 주사위 롤 -> 조정 -> 최종 피해 적용
  - 제안: 주사위 롤 -> 조정 -> 최종 피해를 적에 할당
- 기각 사유:
  - 롤 후 할당은 인지부하를 낮추는 장점이 있으나, 할당 기반 상호작용의 재미를 약화시킴
  - 예: 특정 태그 대상 보너스, 협공 시 보너스 등 "사전 할당"이 만들어내는 긴장/보상이 약해짐
  - 롤 후에도 할당 전/후 수치가 크게 달라질 수 있어, 오히려 "어디에 넣으면 얼마나 오르는지"를 반복 확인하게 되어 인지부하가 다시 증가함
  - 결론적으로 v0 방향에서는 "사전 할당 + 롤 후 조정" 구조를 유지

### 2026-02-09 - 배치 경고/Dead 처리 단순화

- 확정 규칙:
  - 체력 0 이하 `Enemy`는 즉시 보드에서 제거
  - 미배치 경고는 단순 확인 모달로 처리(강제 차단 없음)
- 의도:
  - 불필요한 방어 조건을 줄이고 규칙을 직관적으로 유지
  - 플레이 템포를 끊지 않으면서 실수 방지 UX만 제공

### 2026-02-09 - P2 입력/시각화 규칙 확정

- 확정 규칙:
  - P2 배치는 `드래그 전용`(클릭 배치 미지원)
  - 좌측 모험가 / 우측 Enemy의 대치 레이아웃 사용
  - 할당 상태는 `상시 연결 라인`으로 표시
  - 빈 공간 드롭 시 해당 모험가 배치 해제
  - v0는 모바일 UX를 고려하지 않음(데스크톱 우선)
- 의도:
  - 조작 규칙을 단순화해 학습 비용을 낮춤
  - 배치 관계를 직관적으로 읽게 하여 상황 파악 속도를 높임

### 2026-02-09 - P3/P4 실행 규칙 확정

- 확정 규칙:
  - 리롤/눈 +1 스킬은 `P4`에서만 사용 가능
  - 적 피해 스킬은 `P2`, `P4`에서 사용 가능
  - 같은 적에 배치된 모험가의 공격 순서는 `드래그 배치 순서`를 따름
- 의도:
  - 굴림값이 없는 상태에서의 보정 스킬 사용 혼선을 제거
  - 공격 순서를 전략 요소로 승격해 배치 단계의 의미를 강화

### 2026-02-09 - 모험가 순차 처리 루프 전환 제안(검토중)

- 제안 내용:
  - 기존: 전체 모험가 배치 -> 전체 롤 -> 전체 조정 -> 완료
  - 변경안: 모험가 1명 배치 -> 롤 -> 조정 -> 확정을 반복, 전원 완료 시 턴 종료
- 기대 효과:
  - 플레이어가 중간 결과를 보고 다음 결정을 수정할 수 있어 초기 인지부하 완화 가능
- 우려:
  - 모험가 수만큼 소단계가 반복되어 턴 템포가 늘어질 수 있음
  - 후행 모험가가 선행 결과를 보고 최적 대응하는 구조가 되어 평균 난이도가 하락할 수 있음
  - 기존 "사전 계획 + 순서 전략"의 긴장감이 약화될 가능성
- 상태:
  - `[검토중]`

### 2026-02-09 - 모험가 순차 처리 루프 채택

- 최종 확정:
  - 턴 처리 방식은 "모험가 1명 배치 -> 롤 -> 조정 -> 확정" 반복으로 전환
  - 순차 처리에서 스킬 대상은 현재 처리 중인 모험가 기준으로 제한
  - 리롤/눈 +1은 `P4` 전용 유지
- 채택 이유:
  - 전체 선계획 강도를 낮춰 인지부하 완화
  - 대신 UI/입력 템포를 빠르게 유지하는 것을 핵심 과제로 설정

### 2026-02-09 - 순차 처리 UX/입력 규칙 확정

- 확정 규칙:
  - 모험가 처리 순서는 플레이어가 자유 선택
  - 현재 모험가 확정 후 Undo 불가
  - 적 피해 스킬은 `P2`, `P4` 모두 허용
  - 턴 종료 시 미처리 모험가는 경고 후 강행 가능
  - 입력 템포 목표:
    - 드래그 드롭 즉시 롤
    - `Space`로 확정
    - 스킬은 슬롯 클릭 또는 숫자 단축키 선택 후 타깃 클릭
    - 유효 타깃 하이라이트 표시
  - 스킬 선택 취소는 같은 키 재입력 또는 빈 공간 클릭
  - `확정` 버튼은 `P4`에서만 노출, 하단 중앙(Footer Primary CTA) 배치
  - 정산 연출은 기본 짧게, `빠르게 진행` 제공
- 후속 변경:
  - 아래 `모험가별 굴리기-조정-공격 루프 채택` 결정으로 입력 플로우가 일부 대체됨

### 2026-02-09 - v0 보상 시스템 비활성화

- 확정 규칙:
  - v0에서는 처치 보상/스테이지 보상 시스템을 구현하지 않음
  - `Enemy` 데이터에서 보상 필드를 제거
  - 스테이지 기반 보상 데이터 분리는 차기 설계 항목으로 보류
- 의도:
  - 핵심 전투 루프 검증에 집중하고, 조기 복잡도 증가를 방지

### 2026-02-09 - 전체 롤 후 타게팅 전환 제안 기각(확정)

- 대상 변경안:
  - 기존: 모험가 1명 배치 -> 롤 -> 조정 -> 확정 반복
  - 제안: 모든 모험가 롤 -> 조정 -> 원하는 순서로 Enemy 타게팅/공격
- 기대 효과:
  - 반복 확정 단계를 줄여 턴 템포를 빠르게 유지
  - 플레이어가 결과값을 본 뒤 판단하므로 초기 진입 인지부하 완화
- 핵심 리스크:
  - 순서 의존 효과가 있을 때, 드래그로 여러 Enemy를 훑으며 "최적 데미지 탐색" 노동이 발생할 수 있음
  - 정보가 완전히 공개되면 긴장감이 줄고 퍼즐 풀이 성격이 강해질 수 있음
  - 드래그 중 실시간 재계산 범위를 넓게 잡으면 UI/로직 복잡도가 급증함
- 최종 결론:
  - v0에서는 기존 순차 처리 루프(모험가 1명 배치 -> 롤 -> 조정 -> 확정 반복)를 유지
- 기각 사유:
  - 드래그 탐색 중심 플레이가 계산 노동으로 변할 가능성이 큼
  - 순서 의존 효과가 추가될수록 실시간 프리뷰 복잡도가 급증함
  - 현재 목표(빠른 검증) 대비 구현/검증 비용이 큼
- 상태:
  - `[기각]`

### 2026-02-09 - 모험가별 굴리기-조정-공격 루프 채택(확정)

- 대상 변경안:
  - 변경 전: 모험가 1명 배치 -> 롤 -> 조정 -> 확정 반복
  - 변경 후: 모험가별 `굴리기` -> 조정 -> 드래그 공격 반복
  - 제약: 한 번 굴린 모험가는 반드시 먼저 소비(공격)한 뒤 다음 모험가 처리
- 확정 입력 규칙:
  - 각 모험가는 `굴리기` 버튼과 단축키 `1/2/3/4` 모두 지원
  - `1/2/3/4`는 모험가 슬롯(위->아래) 고정 매핑
  - 스킬 입력은 `Q/W/E/R` 단축키 + 슬롯 클릭 병행
  - 롤 직후에는 다른 모험가의 `굴리기` 버튼/단축키를 비활성화
  - 비활성화된 롤 입력은 별도 경고/피드백 없이 무시
  - 현재 롤된 모험가의 공격이 끝나면 다음 모험가 롤이 다시 활성화
  - 현재 처리 중 모험가는 별도 외곽선/배지 강조 없이 카드 상태 텍스트로만 구분
- 채택 이유:
  - 단계가 명확해져 학습 비용이 낮고 실수 원인 추적이 쉬움
  - 기존 `확정` 반복 버튼 피로를 줄이면서 순차 전략성은 유지 가능
- 상태:
  - `[채택]`

## 기록 규칙

- 상태 태그:
  - `[아이디어]` 아직 검토 전
  - `[검토중]` 시스템 규칙과 충돌 여부 확인 중
  - `[채택]` 현재 기획 기준에 반영됨
  - `[보류]` 재미는 있으나 우선순위 낮음
  - `[기각]` 이번 사이클에서 적용하지 않음(사유 기록 필수)
- 밸런스 수치는 초안이며, 플레이테스트로 조정합니다.
- 핵심 파라미터 확정 전에는 수치 대신 기믹/조건/리스크를 우선 기록합니다.

### 아이디어 기록 템플릿

```md
- `[아이디어]` (이름)
  - 의도: (왜 필요한 요소인지)
  - 기대 재미: (플레이어가 어떤 판단/쾌감을 얻는지)
  - 밸런스 영향: (초반/중반/후반, 오버/언더 가능성)
  - 리스크: (악용 루프, 조합 강제, 의미 없는 선택 등)
  - 개선안: (기믹 조정, 조건 추가, 제약 부여 등)
  - 상태: `[아이디어]` / `[검토중]` / `[채택]` / `[보류]` / `[기각]`
```

## 시스템 아이디어

- `[채택]` 안정도(왕국 체력) 시스템
  - 0 이하가 되면 멸망(게임 오버)
  - 상황 실패/디버프로 감소, 보상/행동으로 회복
- `[채택]` 주사위 분배 기반 해결 구조
  - 매 턴 보유 주사위를 상황 카드 슬롯에 할당 후 굴림
  - 상황마다 주사위 최대 할당 개수 제한 가능
- `[채택]` 주사위 강화 시스템
  - 주사위당 강화 1개 장착
  - 예시: 모든 눈 +1, 리롤 1회 보장, 2회 굴려 높은 눈 사용
- `[채택]` 스킬(Skill) 시스템
  - 액티브 효과 + 턴 쿨다운
  - 전술적 보정(리롤/눈 보정/남은 턴수 조절 등)
- `[채택]` 구조물(Structure) 시스템
  - 런 동안 유지되는 영구 패시브
  - 빌드 방향과 장기 생존력 강화
- `[아이디어]` 오버킬 매커니즘
  - 슬롯을 초과해서 달성한 수치(오버킬)에 반응하는 트리거/보상 시스템
  - 방향성: 고점 주사위 운용과 시너지 빌드의 보상 체감 강화
  - 세부 규칙은 미정(즉시 보상형 vs 누적형 vs 조건형)

- `[아이디어]` 묘지기(모험가)
  - 기믹: 배정된 적이 이미 처치된 경우, 다음 적으로 자동 재지정 후 공격
  - 의도: 과잉 화력으로 인한 공중분해 손실을 부분 완화
  - 리스크: 배치 순서 전략의 의미를 약화하거나 난이도 하향으로 이어질 수 있음
  - 상태: `[아이디어]`

- `[아이디어]` 선봉대(모험가/무기)
  - 기믹: 해당 Enemy를 가장 먼저 공격하면 버프 획득
  - 의도: 배치/공격 순서에 의미를 부여하고 선공 선택의 재미 강화
  - 리스크: 선공 보너스가 과하면 사실상 고정 오프너가 강제될 수 있음
  - 상태: `[아이디어]`

- `[아이디어]` 협공가(모험가)
  - 기믹: 같은 Enemy를 자신보다 먼저 공격한 모험가 수에 비례해 버프 획득
  - 의도: 단일 대상 집중 화력 빌드의 시너지 강화
  - 리스크: 다인 집중이 정답화되면 타깃 분산 전략이 사라질 수 있음
  - 상태: `[아이디어]`

- `[아이디어]` 마무리 사냥꾼(모험가/무기)
  - 기믹: 대상 Enemy 체력이 50% 이하일 때 버프 획득
  - 의도: 전투 중반 이후의 타깃 전환 판단을 유도
  - 리스크: 체력 임계값 확인 피로가 누적될 수 있음
  - 상태: `[아이디어]`

- `[아이디어]` 현상금 집행자(모험가)
  - 기믹: 막타 처치 시 소량 골드 획득
  - 의도: 타격 순서 최적화 보상 제공
  - 리스크: 막타 몰아주기 메타가 과해지면 플레이 다양성이 감소
  - 상태: `[아이디어]`

- `[아이디어]` 분열 타격(Skill)
  - 기믹: 선택한 모험가의 이번 턴 피해가 양옆 Enemy에게도 일부 전이
  - 의도: 광역 대응 수단 제공, 배치 가치 상승
  - 리스크: 다수 Enemy 구간 난이도를 급격히 낮출 위험
  - 상태: `[아이디어]`

## 상황 카드 아이디어

- `[채택]` 메뚜기 떼
  - 타입: 기근
  - 남은 턴수: 2
  - 목표치: 5, 8
  - 실패: 안정도 -1, 3턴 `굶주림` 디버프
  - 굶주림: 기근 상황 추가 실패 시 안정도 -1 추가
  - 성공: 안정도 +1

- `[채택]` 오크 부대
  - 타입: 침략
  - 남은 턴수: 1
  - 목표치: 10 (최대 주사위 3개)
  - 실패: 안정도 -2
  - 성공: 골드 +3

- `[채택]` 영지 순찰
  - 타입: 내정
  - 남은 턴수 없음(상시 행동칸)
  - 목표치: 10
  - 완료: 안정도 +1
  - 완료 후 재생성

- `[아이디어]` 거대 프로젝트
  - 목표치: 50
  - 턴 제한 없음
  - 성공 보상: `웅장한 동상` 패시브 획득

- `[아이디어]` 반란군
  - 기믹: 방치할수록 요구 목표치가 누적 증가하는 위기 카드
  - 의도: 방치 시 리스크가 누적되는 고우선 상황 카드
  - 기대 재미: "지금 처리할지, 다른 위기를 먼저 막을지" 우선순위 판단 압박
  - 밸런스 영향: 조기 대응 시 부담이 낮고, 지연할수록 비용이 커지는 선택 구조를 형성
  - 리스크: 카드를 늦게 뽑았을 때 사실상 해결 불가 상태로 커질 수 있음
  - 개선안:
    - 초기 부담을 낮게 설정해 "빠르게 처리할수록 이득" 구조를 보장
    - 누적 증가 완화 장치(상한, 감소 트리거, 일시 정지 효과) 중 1개 이상 포함
    - 대응 수단(Decree/Skill/Structure) 접근성을 확보해 불합리 누적을 방지
  - 상태: `[아이디어]`

- `[아이디어]` 내부 공작망
  - 기믹: 이 상황이 생성될 때, 스킬 풀 전체에서 무작위 스킬 1개를 "사용 불가"로 봉인
  - 해제 조건: 해당 상황을 해결할 때에만 봉인 해제
  - 의도: 스킬 의존 빌드에 대한 카운터 압박 + 우선순위 전환 강제
  - 기대 재미: "핵심 카드를 살릴지, 다른 위기를 먼저 막을지"의 고통스러운 선택
  - 밸런스 영향: 특정 빌드의 고점을 눌러 평균 난이도를 높일 수 있음
  - 리스크:
    - 핵심 스킬 봉인 시 대응 수단이 사라져 런 파괴감이 커질 수 있음
    - 운 나쁨 체감이 강해지면 공정성 인식이 빠르게 악화될 수 있음
  - 개선안:
    - 봉인 대상 정보(누가 봉인됐는지/해제 조건)를 카드와 HUD에 상시 노출
    - 봉인/해제 로그를 턴 로그에 남겨 피격 원인을 추적 가능하게 유지
  - 상태: `[아이디어]`

## 스킬/구조물/칙령 아이디어

- `[아이디어]` 은둔 현자 (Skill)
  - 효과: 1이 나온 주사위에만 사용 가능, 해당 주사위 +3
  - 쿨다운: 2
  - 플레이버: 그를 아는 이는 적으나, 그가 쓴 글은 세상을 움직인다.

- `[아이디어]` 첩 (Skill)
  - 효과: 선택한 주사위 리롤
  - 쿨다운: 1
  - 플레이버: 그녀에게는 여러 이름이 있다. 장막 뒤의 단검. 왕의 세 번째 눈. 침실의 스킬.

- `[아이디어]` 배치 전환관 (Skill)
  - 기믹: `P4`에서 서로 다른 `Situation`에 배치된 주사위 2개의 위치를 교환
  - 의도: `P2` 배치 실수의 복구 수단 제공 + 고급 전술 선택지 추가
  - 리스크: 기본 규칙의 "상황 잠금" 긴장감을 약화할 수 있음
  - 개선안:
    - 쿨다운/사용 조건을 부여해 상시 재배치화 방지
    - 같은 턴 내 1회 또는 상황당 1회 제한
  - 상태: `[아이디어]`

- `[보류]` 무쿨다운 비용형 Skill
  - 기믹: 쿨다운 대신 골드를 소모해 즉시 효과 발동
  - 의도: 비상 상황에서 골드를 전술 파워로 교환하는 선택 제공
  - 리스크: 골드 수급이 안정되면 사실상 상시 버프가 될 수 있음
  - 개선안:
    - 글로벌 하드 제한 대신, 개별 스킬별 비용 구조로 위험도 제어
    - 예시: `용병단장`은 사용 시마다 비용이 1씩 증가
    - 특정 태그 상황에서만 사용 가능하게 설계해 범용성 과다를 방지
  - 보류 사유: v0 정책(모든 스킬은 개별 쿨다운 + 턴당 1회)과 충돌
  - 상태: `[보류]`

- `[아이디어]` 용병단장 (Skill)
  - 기믹: 선택한 주사위 눈 +1, 사용 시 골드 소모
  - 의도: 골드를 즉시 전투력으로 치환하는 전술형 버튼
  - 리스크: 골드 수급과 결합 시 반복 사용 효율이 과도해질 수 있음
  - 개선안:
    - 사용할수록 비용 증가(누진)
    - 특정 턴/상황에서만 추가 보정 가능
  - 상태: `[아이디어]`

- `[채택]` 스킬 용어 통일(영문 우선)
  - 기믹: 스킬을 "인물" 테마로 묶는 명명 규칙 적용
  - 확정 용어: `Skill`
  - 의도: 세계관 몰입과 인지 일관성 강화
  - 리스크: 용어가 추상적이면 기능 직관성이 떨어질 수 있음
  - 개선안:
    - 카테고리명은 `Skill`로 고정하고
    - 카드 타이틀/아이콘에서 기능 타입을 보조 표기
  - 상태: `[채택]`

- `[아이디어]` 스킬 슬롯 가중치 시스템
  - 기믹: 스킬마다 점유 슬롯이 다르고, 플레이어는 총 슬롯 한도 내에서 구성
  - 의도: 단순 개수 제한보다 빌드 의사결정 밀도 강화
  - 리스크: UI/인지 복잡도 증가, 초반 진입장벽 상승
  - 개선안:
    - 초반에는 슬롯 비용 1 위주로 시작해 점진적으로 고비용 스킬 해금
    - 덱빌딩 UI처럼 현재 점유/잔여 슬롯을 항상 시각화
  - 상태: `[아이디어]`

- `[아이디어]` 국경수비대 (Structure)
  - 효과: 모든 `침략` 태그 상황의 시작 목표치 -1

- `[아이디어]` 최후의 의지 (Structure)
  - 기믹: 안정도가 0 아래로 내려가도 1턴 유예 상태로 생존
  - 의도: 역전 기회 제공, 극단 턴에서의 드라마 강화, 더 과감한 리스크 선택 유도
  - 리스크: 사실상 추가 목숨처럼 작동해 난이도 체감을 왜곡할 수 있음
  - 개선안:
    - 획득 난이도를 높게 설정해 접근성을 제한
    - 런당 1회 한정
    - 유예 턴 종료 시 안정도 1 이상 회복 실패하면 즉시 게임 오버
  - 상태: `[아이디어]`

- `[아이디어]` 은행 (Structure)
  - 기믹: 골드를 `-N`까지 대출(적자) 가능하게 확장
  - 의도: 단기 부채를 통한 고위험 고보상 운영 허용
  - 리스크: 대출이 너무 강하면 사실상 공짜 골드로 변질
  - 개선안:
    - 효과 텍스트는 단일 규칙(대출 한도)로 유지해 직관성을 우선
    - UI에 현재 골드와 대출 한도(`-N`)를 상시 표시
    - 중복 획득 시 대출 한도는 중복 적용(누적)
  - 상태: `[검토중]`

- `[채택]` 구조물 용어 전환(영문 우선)
  - 기믹: "유산"을 구조물 명칭으로 교체
  - 확정 용어: `Structure`
  - 의도: 왕국 경영 판타지와 시스템 의미를 더 직관적으로 연결
  - 리스크: 용어가 너무 일반적이면 스킬/칙령과 경계가 흐려질 수 있음
  - 개선안:
    - 카테고리 용어 + 서브타입(예: Military, Civic, Faith) 동시 표기
  - 상태: `[채택]`

- `[아이디어]` 용병단 고용서 (Decree)
  - 대상: `침략` 태그 상황만 사용 가능
  - 효과: 해당 상황 목표치 -4
  - 테마: 칙령/문서형 소모품

- `[아이디어]` 웅장한 동상 (패시브)
  - 효과: 매 3턴마다 안정도 +1
  - 획득처: 거대 프로젝트 완료 보상

## 진행 예시 메모

- 플레이어 기본 상태 예시:
  - 주사위 4개(기본 3, 강화 1)
  - 스킬 3개(리롤 CD1, 눈 +1 CD1, 상황 요구치 -2 CD2)
  - 구조물 1개(이번 턴 최저 눈 +1)
- 전술 포인트:
  - 단기적으로 실패를 허용하고 핵심 실패를 지연시키는 선택
  - 고가치 상황 우선 해결 vs 안정도 보호용 행동 선택

## 오픈 질문

- 스테이지 단위 + 준비시간 액션 구조를 2차 실험에 언제 전환할지
- 2차 사이클 난이도 상승 규칙(신규 생성, 몬스터 강화, 액션 압박 중 우선 축)
- v0 프리셋 3개의 실제 조합안 확정
- 추가 몬스터 2종(`bandit_smugglers`, `siege_shaman`)의 수치 튜닝 기준 확정
- 타입 태그별 전용 디버프/보상 풀 구성
- 성장 보상 주기(몇 턴마다 강화/스킬/구조물 선택 기회 제공할지)
- 골드의 사용처(상점, 즉시 회복, 확률 이벤트 등)
- 오버킬 보상 규칙(남는 수치 이월, 추가 골드 획득, 전용 트리거 여부)
- 증가형 몬스터 위협의 누적 강도 곡선과 완화 장치(상한/감소/정지) 기준
- 은행형 대출 한도(`N`) 값 확정

